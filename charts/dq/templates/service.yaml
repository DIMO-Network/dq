apiVersion: v1
kind: Service
metadata:
  name: {{ include "dq.fullname" . }}
  labels:
    {{- include "dq.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  {{- range $key, $value := .Values.service.ports }}
    - name: {{ $key }}
{{ toYaml $value | indent 6 }}
  {{- end }}
  selector:
    {{- include "dq.selectorLabels" . | nindent 4 }}
